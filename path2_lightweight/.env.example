# ============================================================
# 盘后 daily --live 环境变量 (敏感信息, 不入 git)
# ============================================================
# CTP 账号
CTP_INVESTOR_ID=260042
CTP_PASSWORD=xibeilang@99
CTP_FRONT_ADDR=tcp://182.254.243.31:40001
CTP_BROKER_ID=9999
CTP_APP_ID=simnow_client_test
CTP_AUTH_CODE=0000000000000000
CTP_FLOW_PATH=./ctp_flow_daily/
CTP_TIMEOUT=15

# 飞书 (lark-cli 模式, 0 配置, 走 user 身份发到自己的 P2P, 已实测通过)
# lark-cli 已配过 user 身份 (ou_xxx open_id 会自动从 chat-list 拿)
# 如需手动指定, 设 LARK_USER_OPEN_ID
# LARK_USER_OPEN_ID=ou_xxxxxxxx

# 飞书 webhook 群机器人模式 (可选, 不配则 skip 飞书 webhook 推送, 跟钉钉一样)
# 1. 飞书群 -> 设置 -> 群机器人 -> 添加机器人 -> 自定义机器人
# 2. 复制 webhook URL (https://open.feishu.cn/open-apis/bot/v2/hook/xxx)
# LARK_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_TOKEN

# 钉钉 (可选, 不配则 skip 钉钉推送)
# 1. 钉钉群 -> 群设置 -> 智能群助手 -> 添加机器人 -> 自定义 (加签)
# 2. 复制 webhook URL
# DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
# DINGTALK_SECRET=SECxxx  (如果开了加签)

# Bark iOS (可选, 不配则 skip)
# BARK_KEY=YOUR_BARK_KEY
# BARK_SERVER=https://api.day.app
